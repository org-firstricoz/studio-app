part of '../pages/user_chat_profile.dart';

class ChatMutePopupOptions extends StatelessWidget {
  const ChatMutePopupOptions({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [],);
  }
}